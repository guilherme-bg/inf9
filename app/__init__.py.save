from flask import Flask
from config import app_config
import os

config_name = os.environ.get('FLASK_ENV')

def create_app():
    app = Flask()
    app.config_from_object(app_config[config_name~)
    return app

    @approute('/')
    def index():
        return 'HELLO!'
    return app
